import React from "react";
import Image from "next/image";

const Dashboard = (props: React.SVGProps<SVGSVGElement>) => {
  return <Image src="/svg/dashboard.svg" height={24} width={24} alt=" Image" />;
};

export default Dashboard;
