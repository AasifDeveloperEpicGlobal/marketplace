import React from "react";
import Image from "next/image";

const Resend = (props: React.SVGProps<SVGSVGElement>) => {
  return <Image src="/svg/resend.svg" height={20} width={20} alt=" Image" />;
};

export default Resend;
